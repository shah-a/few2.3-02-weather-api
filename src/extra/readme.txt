I wanted to use this loader, but didn't end up implementing it.

I got it from the tailwindcss docs website (but needed to convert it to jsx).
It was being used as a demo for the `animate-spin` property.
